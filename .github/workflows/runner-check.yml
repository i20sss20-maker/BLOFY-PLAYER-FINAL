name: Runner Check

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Runner is alive
        run: |
          echo "BLOFY FINAL runner OK"
          java -version
          echo "ANDROID_HOME=$ANDROID_HOME"
